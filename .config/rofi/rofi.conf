/* Rofi Configuration - Generated by theme switcher (Simple) */

configuration {
    modi: "drun,run,window";
    show-icons: true;
    icon-theme: "Papirus-Dark";
    terminal: "kitty";
    drun-display-format: "{name}";
    font: "JetBrainsMono Nerd Font 12";
    display-drun: "Apps";
    display-run: "Run";
    display-window: "Windows";
}

window {
    width: 500px;
    border: 2px solid;
    border-color: #7aa2f7;
    border-radius: 15px;
    background-color: #1a1b26;
}

mainbox {
    background-color: transparent;
    children: [inputbar, listview];
}

inputbar {
    background-color: #24283b;
    border-radius: 10px;
    margin: 10px;
    padding: 10px;
    children: [prompt, entry];
}

prompt {
    background-color: transparent;
    text-color: #7aa2f7;
    margin: 0px 10px 0px 0px;
}

entry {
    background-color: transparent;
    text-color: #c0caf5;
    placeholder: "Search...";
    placeholder-color: #565f89;
}

listview {
    background-color: transparent;
    margin: 0px 10px 10px 10px;
    lines: 8;
    columns: 1;
    spacing: 5px;
}

element {
    background-color: transparent;
    padding: 10px;
    border-radius: 10px;
}

element-icon {
    size: 24px;
    margin: 0px 10px 0px 0px;
}

element-text {
    text-color: #c0caf5;
}

element selected {
    background-color: #364a82;
}

element selected element-text {
    text-color: #c0caf5;
}
