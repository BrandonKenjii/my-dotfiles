-- Input Configuration

hl.config({
    input = {
        kb_layout = "us",

        follow_mouse = 1,
        sensitivity = 0,

        touchpad = {
            natural_scroll = true,
            scroll_factor = 0.35,
            tap_to_click = true,
            drag_lock = 1,
            disable_while_typing = true,
        },
    },
})
